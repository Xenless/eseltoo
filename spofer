local mt = getrawmetatable(game)
local imt = mt.__index

function xenSpoof(Path, Property, Value)
setreadonly(mt, false) 
mt.__index = function(object, property)
    if object == Path and property == Property then
        return Value
    end
    return imt(object, property)
end
end
